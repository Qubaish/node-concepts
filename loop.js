const pendingTimes = [];
const pendingOsTasks = [];
const pendingOperations = [];

function shouldContinue(){
    // check pending timers, (setimeout)
    // check pending OS tasks ? (server listening)
    // check pending long running operatiosn (fs module)
};

while(shouldContinue()){

};